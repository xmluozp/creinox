import {enums} from './enum'

export const _DATATYPES = {
    INT: "INT",
    VARCHAR: "VARCHAR",
    DATETIME: "DATETIME",
    TEXT: "TEXT",
    BOOLEAN: "BOOLEAN",
    SELECT: "SELECT",
    GALLERY: "GALLERY",
    ENUM: enums
}